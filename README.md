# network_monitoring_dashboard
A web-based dashboard that visualizes real-time network data such as: Ping latency to common hosts (e.g., Google DNS, local gateway) Download/upload speed (optional via API or dummy data) Device IP and MAC addresses (simulated list) System uptime Alerts for connectivity issues
